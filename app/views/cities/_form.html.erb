<div class="containter">

<%= form_for @city, class: "form-group" do |f| %>
  <% if @city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@city.errors.count, "error") %> prohibited this city from being saved:</h2>

      <ul>
      <% @city.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :country_id,class: "label" %><br>
    <%= f.collection_select :country_id, Country.all, :id, :name , {prompt: "Select a category"}, {class: "selectpicker"}%>
  </div>
  <div class="field">
    <%= f.label :name,class: "label" %><br>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  <div class="field">
    <%= f.label :image, class: "label" %><br>
    <%= f.text_field :image, class: "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit 'Save', class: "btn btn-info crud" %>
  </div>
<% end %>

</div>